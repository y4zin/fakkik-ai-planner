# ربط حسابات «فكّك» وتخزينها بصورة مستقلة

## القرار المقترح

واجهة الحساب التي أضيفت تحمل اسم **فكّك** فقط. للخدمة الخلفية أقترح ربطها بـ **Supabase Auth + Postgres**: ينشئ المستخدم حسابًا بالبريد وكلمة المرور، ثم تُربط محادثاته وخططه وذكرياته بمعرّف حسابه بدل معرّف المتصفح المحلي.

> **لا يوجد في واجهة فكّك أي وسم ظاهر لمنصة الاستضافة أو قاعدة البيانات.** ومع ذلك، ينبغي معرفة أن خطة Supabase المجانية لا تتضمن إزالة علامة Supabase من رسائل المصادقة الافتراضية؛ لهذا نستخدم رسائل تحمل هوية فكّك فقط عند توفير SMTP خاص أو خطة تدعم إزالة العلامة.[1]

| المسار | ما يحصل عليه المستخدم | الكلفة المبدئية | التعقيد | الملاحظة |
|---|---|---:|---|---|
| **Supabase Auth + Postgres** | حساب بريد/كلمة مرور وتخزين دائم مع سياسات وصول لكل مستخدم | مجاني للبداية | منخفض | الخيار المقترح؛ الخطة المجانية تتضمن قاعدة 500MB و50,000 مستخدم نشط شهريًا، لكنها توقف المشروع بعد أسبوع من عدم النشاط.[1] |
| **Supabase ذاتي الاستضافة** | نفس تجربة الحساب مع سيطرة كاملة على الرسائل والبنية | ليست مجانية بالضرورة بسبب الخادم | مرتفع | مناسب لاحقًا فقط إذا أردت تحكمًا تقنيًا كاملاً في البنية والرسائل. |

## ما سنربطه بعد موافقتك

1. تنشئ مشروع Supabase باسم مشروعك أنت، ثم تفعّل **Email + Password**. هذه المصادقة مفعلة افتراضيًا في المشاريع المستضافة، مع إمكانية طلب تأكيد البريد قبل الدخول.[2]
2. تضبط رابط التطبيق النهائي ضمن Redirect URLs حتى تعود رسالة التأكيد إلى دومين فكّك الصحيح.[2]
3. تضيف مفاتيح المشروع الآمنة عبر إعدادات المشروع، وليس داخل الكود أو GitHub.
4. ننقل الجداول الحالية إلى قاعدة Supabase ونطبق قواعد وصول صفّية، بحيث لا يقرأ المستخدم إلا خططه وجلساته وذاكرته.
5. ننشر الواجهة والخادم على استضافة Node مناسبة. **GitHub Pages وحدها لا تشغّل الخادم أو قاعدة البيانات أو منطق الذكاء الاصطناعي**؛ هي مناسبة فقط للواجهة الثابتة.

## تنبيه الذكاء الاصطناعي عند النشر المستقل

منطق التخطيط الحالي يستدعي نموذجًا من الخادم. عند فصل التطبيق عن بيئة التطوير الحالية، نربطه بمفتاح مزود ذكاء اصطناعي تختاره أنت ونحفظه سرًا في إعدادات الخادم. لا يُرسل المفتاح إلى المتصفح أو GitHub.

## ما أحتاجه لتفعيل الدخول الحقيقي

بعد أن تنشئ مشروع Supabase، أحتاج فقط إلى **Project URL** و**Publishable/Anon Key**؛ أما مفاتيح الخادم الحساسة فتبقى في إعدادات الخادم. عندها يتحول زر الحساب من واجهة تصميم إلى تسجيل وإنشاء حساب فعليين.

## المراجع

[1]: https://supabase.com/pricing "Supabase Pricing"
[2]: https://supabase.com/docs/guides/auth/passwords "Supabase Auth: Password-based Auth"
