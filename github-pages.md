# GitHub Pages — واجهة «فكّك» العامة

تُنشر الواجهة الثابتة إلى GitHub Pages تلقائيًا عند أي دفع إلى الفرع `main`. رابط Pages مفيد لمشاهدة الهوية والواجهة من الهاتف، لكنه **ليس خادم الذكاء الاصطناعي**: لا يحتفظ بمفتاح النموذج، ولا يشغل قاعدة البيانات، ولا ينفذ tRPC/Express.

## ما يعمل على Pages الآن

تعمل الواجهة والأنماط والتنقل المحلي. تحتاج أزرار المحادثة والخطط والحفظ والجلسات إلى عنوان خادم مستقل قبل أن تصبح وظيفية على الرابط العام.

## المسار المجاني المقترح

| الجزء | الخدمة المقترحة | الغرض | الحد الذي يجب معرفته |
|---|---|---|---|
| الواجهة | GitHub Pages | رابط الموقع الثابت | لا يشغّل منطق الخادم أو الأسرار. |
| الخادم | Cloudflare Worker | تمرير طلبات التخطيط وحماية مفتاح النموذج | الطبقة المجانية تحدها 100,000 طلب يوميًا و10ms CPU لكل طلب؛ انتظار استدعاء نموذج خارجي لا يحسب CPU.[1] |
| الذكاء | Gemini Flash عبر Google AI Studio | إنشاء الخطط المنظمة | الطبقة المجانية لها نماذج وحصص محدودة وقد تُستخدم البيانات لتحسين المنتجات؛ ليست بلا حدود.[2] |
| الحساب والبيانات | Supabase Auth + Postgres | بريد/كلمة مرور وعزل بيانات المستخدمين | 50,000 مستخدم نشط شهريًا و500MB قاعدة بيانات مجانًا، ويتوقف المشروع المجاني بعد أسبوع عدم نشاط.[3] |

## قبل تشغيل الذكاء الاصطناعي العام

يُحفظ مفتاح Gemini في أسرار Worker فقط، وليس في GitHub أو ملفات الواجهة. بعد إنشاء حسابي Cloudflare وGoogle AI Studio، تُضاف قيمة `GEMINI_API_KEY` كسر للخادم، ويُضبط عنوان Worker في الواجهة. بعد ذلك يعمل التخطيط من الرابط العام من دون الاعتماد على بيئة التطوير.

## المراجع

[1]: https://developers.cloudflare.com/workers/platform/limits/ "Cloudflare Workers limits"
[2]: https://ai.google.dev/gemini-api/docs/pricing "Gemini Developer API pricing"
[3]: https://supabase.com/pricing "Supabase pricing"
