# النشر وتسجيل المستخدمين

## ما يقدمه GitHub Pages

GitHub Pages يستضيف ملفات الواجهة الساكنة فقط، أي HTML وCSS وJavaScript. لا يشغل خادم Express أو tRPC أو اتصالات MySQL، ولا يحتفظ بحسابات المستخدمين أو جلسات تسجيل الدخول بنفسه. رفع الكود إلى GitHub لا ينشئ تسجيل دخول تلقائيًا.

## المسار الملائم لتطبيق فكّك

يبقى النشر على Manus هو المسار الأبسط للتطبيق الحالي، لأن خادم التخطيط وقاعدة البيانات والمصادقة موجودة بالفعل مع المشروع. عند تفعيل الدخول، يمكن ربط المحادثات والذاكرة والجلسات بحساب المستخدم بدل معرّف المتصفح المحلي.

إذا كان GitHub Pages شرطًا، يجب فصل الواجهة عن الخادم ثم ربطها بخدمة مستقلة للمصادقة وقاعدة البيانات، مثل Supabase أو Firebase، وإعادة بناء استدعاء التخطيط على خادم آمن أو وظيفة سحابية. لا توضع مفاتيح النموذج اللغوي في واجهة GitHub Pages.
